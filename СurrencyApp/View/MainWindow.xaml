<Window x:Class="CurrencyViewer.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:CurrencyViewer.Views"
        Title="Currency Viewer" Height="350" Width="525">
    <Grid>
        <ListView ItemsSource="{Binding Currencies}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Date" DisplayMemberBinding="{Binding Date}" Width="100"/>
                    <GridViewColumn Header="Abbreviation" DisplayMemberBinding="{Binding Abbreviation}" Width="100"/>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="200"/>
                    <GridViewColumn Header="Official Rate" DisplayMemberBinding="{Binding OfficialRate}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button Content="Load Currencies" Command="{Binding LoadCurrenciesCommand}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="120"/>
    </Grid>
</Window>